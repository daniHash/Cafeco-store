{
  "name": "server",
  "private": true,
  "scripts": {
    "start": "./node_modules/.bin/json-server --watch db.json --host 0.0.0.0 --port $PORT",
    "server": "json-server --watch ./db.json --port 8000 delay 500"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
